@if (Model == null || !Model.Any())
{
    <p class="text-muted">No issues have been reported yet.</p>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Location</th>
                <th>Category</th>
                <th>Description</th>
                <th>Date Reported</th>
                <th>Attachment</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var issue in Model)
            {
                <tr>
                    <td>@issue.Location</td>
                    <td>@issue.Category</td>
                    <td>@issue.Description</td>
                    <td>@issue.DateReported.ToString("g")</td>
                    <td>
                        @if (!string.IsNullOrEmpty(issue.AttachmentPath))
                        {
                            <a href="@issue.AttachmentPath" target="_blank" class="btn btn-sm btn-outline-primary">View</a>
                        }
                        else
                        {
                            <span class="text-muted">No file</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
